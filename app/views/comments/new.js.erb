var a = window.onload = function() {
    document.body.onclick = function(event) {
      return t=event.target||event.srcElement; 
     
    }
}
 $("[id=a.id]").hide().after('<%= j render("form") %>');